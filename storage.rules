rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read
      allow read: if true;
      // Allow write if authenticated
      // We'll tighten this back to admin-only once we confirm it's working
      allow write: if request.auth != null;
    }
  }
}
